# ── Orchestration ────────────────────────────────────────────────────
langgraph>=1.1.0
langsmith>=0.7.0

# ── LLM (NVIDIA API — OpenAI-compatible) ────────────────────────────────────
openai>=1.0.0

# ── Database ─────────────────────────────────────────────────────────
fdb==2.0.4

# ── Data processing ──────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.3.0
scipy>=1.11.0

# ── Visualization (from friend's project) ──────────────────────────
plotly>=5.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
altair>=4.2.0

# ── Excel/Parquet support ────────────────────────────────────────
openpyxl>=3.0.0
pyarrow>=10.0.0

# ── API server ────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# ── Utilities ────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
tqdm>=4.60.0
orjson>=3.9.0

# ── UI (Streamlit approval gates) ─────────────────────────────────
streamlit>=1.29.0

# ── LangChain (for friend's EDA tools) ───────────────────────────
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
